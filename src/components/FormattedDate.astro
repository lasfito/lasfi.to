---
const { date } = Astro.props;
---

<time datetime={date.toISOString()} class="opacity-60">
	{
		date &&
			date?.toLocaleDateString('es-MX', {
				year: 'numeric',
				month: 'short',
				day: 'numeric',
			})
	}
</time>
